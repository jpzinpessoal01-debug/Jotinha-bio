const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const glow=document.getElementById('cursorGlow');addEventListener('pointermove',e=>{if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'}});document.querySelectorAll('.tilt').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${-y*5}deg) rotateY(${x*7}deg) translateY(-3px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});document.querySelectorAll('[data-copy]').forEach(btn=>btn.addEventListener('click',async()=>{const value=btn.dataset.copy;try{await navigator.clipboard.writeText(value);const t=document.getElementById('copyText');if(t){t.textContent='copiado ✓';setTimeout(()=>t.textContent='wwwwerror',1500)}}catch{}}));const loader=document.getElementById('loader');addEventListener('load',()=>setTimeout(()=>loader?.classList.add('hide'),1550));setTimeout(()=>loader?.classList.add('hide'),2600);const c=document.getElementById('bg'),ctx=c.getContext('2d');let dots=[];function resize(){c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);dots=Array.from({length:Math.min(150,Math.floor(innerWidth/5))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.45+.25,vx:(Math.random()-.5)*.045,vy:(Math.random()-.5)*.045,a:.12+Math.random()*.62,phase:Math.random()*Math.PI*2,speed:.012+Math.random()*.025}))}resize();addEventListener('resize',resize);(function draw(t=0){ctx.clearRect(0,0,innerWidth,innerHeight);for(const p of dots){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>innerWidth)p.vx*=-1;if(p.y<0||p.y>innerHeight)p.vy*=-1;const twinkle=.35+.65*((Math.sin(t*p.speed+p.phase)+1)/2);ctx.beginPath();ctx.shadowBlur=p.r>1.1?8:3;ctx.shadowColor='rgba(165,180,255,.8)';ctx.fillStyle=`rgba(180,190,255,${p.a*twinkle})`;ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()}ctx.shadowBlur=0;requestAnimationFrame(draw)})();