export default async function handler(req,res){res.setHeader('Cache-Control','no-store, max-age=0');try{const response=await fetch('https://api.counterapi.dev/v1/jotinha-bio-public/visits/up',{headers:{'accept':'application/json'}});if(!response.ok)throw new Error(`counter ${response.status}`);const data=await response.json();const count=data.count??data.value??data?.data?.count??data?.data?.value;if(count===undefined)throw new Error('invalid counter response');return res.status(200).json({count:Number(count)});}catch(error){return res.status(502).json({error:'counter_unavailable'});}}